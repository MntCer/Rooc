struct Point {
    x: int;
    y: int;
};

fun create_point(x:int, y:int) -> Point {
    return Point {x = x, y = y};
};

fun main() -> int {
    var p = create_point(3, 4);
    print_int(p::x);
    print_int(p::y);
    return 0;
};