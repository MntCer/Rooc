//Test while loop and negative number, fail becau of var count:int = 0 can't be written in one line

fun main() -> int
{
    var count:int = 0;
    while(count > -3){
        count = count-1;
    }
    return count;
}